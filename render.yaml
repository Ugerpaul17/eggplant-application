services:
    - type: web
      name: eggplant-application
      env: python
      plan: free
      buildCommand: |
          pip install --upgrade pip
          pip install -r requirements.txt
      startCommand: |
          gunicorn api.main:app
      envVars:
          - key: PYTHON_VERSION
            value: 3.11.9
