services:
    - type: web
      name: eggplant-application
      env: python
      plan: free
      buildCommand: 'pip install -r requirements.txt'
      startCommand: 'gunicorn api.src.main:app'
      envVars:
          - key: PYTHON_VERSION
            value: 3.11.9
