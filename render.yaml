services:
    - type: web
      name: eggplant-application
      env: python
      plan: free
      rootDir: api
      buildCommand: 'pip install -r requirements.txt'
      startCommand: 'gunicorn src.main:app'
      envVars:
          - key: PYTHON_VERSION
            value: 3.11.9
